# Params used in paper
#' @export
pi_vec_I <- data.table::data.table(pi = c(0,2/3,0,1/3),
                       ad = c(1,2,3,Inf))

pi_vec_II <- data.table::data.table(pi = c(0,.5,.4,.1),
                        ad = c(1,2,3,Inf))

pi_vec_III <- data.table::data.table(pi = c(0,1/2,1/2),
                         ad = c(1,2,Inf))
